# Tag a specific Docker release version as latest

name: docker-tag-latest

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version to tag as latest (e.g., v1.8.4)'
        required: true
        type: string
      tag_rsil:
        description: 'Tag rsil image as latest'
        required: false
        type: boolean
        default: true
env:
  DOCKER_USERNAME: ${{ github.actor }}

permissions: {}

jobs:
  tag-rsil-latest:
    name: Tag rsil as latest
    runs-on: ubuntu-24.04
    if: ${{ inputs.tag_rsil }}
    permissions:
      packages: write
      contents: read
    steps:
      - name: Log in to Docker
        env:
          DOCKER_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "$DOCKER_PASSWORD" | docker login ghcr.io --username "${DOCKER_USERNAME}" --password-stdin

      - name: Pull rsil release image
        env:
          VERSION: ${{ inputs.version }}
        run: |
          docker pull ghcr.io/${{ github.repository_owner }}/rsil:${VERSION}

      - name: Tag rsil as latest
        env:
          VERSION: ${{ inputs.version }}
        run: |
          docker tag ghcr.io/${{ github.repository_owner }}/rsil:${VERSION} ghcr.io/${{ github.repository_owner }}/rsil:latest

      - name: Push rsil latest tag
        run: |
          docker push ghcr.io/${{ github.repository_owner }}/rsil:latest
