import Summary from './SUMMARY.mdx';

# CLI Reference

The Rsil node is operated via the CLI by running the `rsil node` command. To stop it, press `ctrl-c`. You may need to wait a bit as Rsil tears down existing p2p connections or performs other cleanup tasks.

However, Rsil has more commands:

<Summary />
